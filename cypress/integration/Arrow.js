context('Arrow', () => {
  beforeEach(() => {
    cy.navigate([/^Components$/, /^Arrow$/]);
  });

  specify('can be open or closed', () => {});
  specify('can be disabled', () => {});
  specify('defaults to a button, but can also be a span', () => {});
});
