---
name: HoverIntersection
menu: Components
---

import { Component } from 'react';
import { Playground, PropsTable } from '@versionone/doc-components';
import { SpacedGroup } from '../SpacedGroup';
import { createComponent } from '../StyleProvider';
import { Hoverable } from '../Hoverable';
import { HoverIntersection, HoverIntersectionExclude } from './';

Tracks hover state for a component; remove hover state from child intesection regions.

## Basic Example

<Playground>
  {() => {
    const ColorDiv = createComponent(
      ({ hovered }) => ({
        backgroundColor: hovered ? 'lightblue' : 'white',
        padding: 10,
      }),
      'div',
      ['onMouseEnter', 'onMouseLeave'],
    );
    return (
      <HoverIntersection>
        {({ bind, hovered }) => (
          <ColorDiv {...bind} hovered={hovered}>
            <p>parent div</p>
            <HoverIntersectionExclude>
              {({ bind, hovered }) => (
                <ColorDiv {...bind} hovered={hovered}>
                  child div
                </ColorDiv>
              )}
            </HoverIntersectionExclude>
          </ColorDiv>
        )}
      </HoverIntersection>
    );
  }}
</Playground>

## API

### IntersectionHover

<PropsTable of={HoverIntersection} />

### IntersectionHover.Exclude

<PropsTable of={HoverIntersectionExclude} />
