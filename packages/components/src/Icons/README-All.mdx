---
name: All Icons
route: /theme/icons/all
menu: Icons
---

import { Tooltip } from '../Tooltip';
import { Playground, PropsTable } from 'docz';
import { DeleteIcon } from './';
import * as AllIcons from './';
import SpacedGroup from './../SpacedGroup';
import { Grid, Column } from '../Grid';
import { CopyToClipboard } from '../CopyToClipboard';
import { palette } from '../palette';

# All Icons

If you don't see your icon, go to [here](/theme/icons) to for instructions on adding one in.

<Playground>
	{() => {
		const tile = {
			display: 'flex',
			alignItems: 'center',
			justifyContent: 'center',
			width: '24px',
			height: '24px',
		}

    	return <SpacedGroup xs={4}>
    		{Object.keys(AllIcons).map(iconKey => {
    			const Icon = AllIcons[iconKey];
    			const anchor = (
    				<Icon color={palette.gunmetal} />
    			);
    			return (
    				<div key={iconKey} style={tile}>
    					<Tooltip anchor={anchor}>
    						<SpacedGroup xs={4}>
    							<span>{iconKey}</span>
    						</SpacedGroup>
    					</Tooltip>
    				</div>
    			)
    		})}
    	</SpacedGroup>
    }}

</Playground>
