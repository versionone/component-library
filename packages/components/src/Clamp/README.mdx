---
name: Clamp
menu: Components
route: /components/Clamp
---

import { Playground, PropsTable } from '@versionone/doc-components';
import { Link } from '../Link';
import { Clamp } from './';

# Clamp

Clamp textual content to a single line with an ellipsis.

## Textual Content

<Playground>
  <Clamp data-test="too-short-to-clamp">Hello world, I'm not clamped</Clamp>
  <Clamp data-test="clamp-text">
    This is a very lengthy bit of textual content. This is just text, no markup,
    but can be truncated via the Clamp component. This is a very lengthy bit of
    textual content. This is just text, no markup, but can be truncated via the
    Clamp component. This is a very lengthy bit of textual content. This is just
    text, no markup, but can be truncated via the Clamp component. This is a
    very lengthy bit of textual content. This is just text, no markup, but can
    be truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component. This is a very
    lengthy bit of textual content. This is just text, no markup, but can be
    truncated via the Clamp component. This is a very lengthy bit of textual
    content. This is just text, no markup, but can be truncated via the Clamp
    component. This is a very lengthy bit of textual content. This is just text,
    no markup, but can be truncated via the Clamp component.
  </Clamp>
  <Clamp data-test="clamp-text-no-whitespace">
    Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.Thisisaverylengthybitoftextualcontent.Thisisjusttext,nomarkup,butcanbetruncatedviatheClampcomponent.
  </Clamp>
</Playground>

## API

<PropsTable of={Clamp} />
