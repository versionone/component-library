---
name: Rotate
route: /components/Rotate
menu: Components
---

import { Playground, PropsTable } from '@versionone/doc-components';
import { Rotate } from './';
import { SpacedGroup } from '../SpacedGroup';
import { Avatar } from '../Avatar';
import { ChevronIcon } from '@versionone/icons';

# Rotate

<Playground>
  <SpacedGroup center>
    <SpacedGroup direction="direction" center>
      <Rotate deg={0}>
        <Avatar icon={<ChevronIcon />} />
      </Rotate>
      <span>Right</span>
    </SpacedGroup>
    <SpacedGroup direction="direction" center>
      <Rotate deg={90}>
        <Avatar icon={<ChevronIcon />} />
      </Rotate>
      <span>Down</span>
    </SpacedGroup>
    <SpacedGroup direction="direction" center>
      <Rotate deg={180}>
        <Avatar icon={<ChevronIcon />} />
      </Rotate>
      <span>Left</span>
    </SpacedGroup>
    <SpacedGroup direction="direction" center>
      <Rotate deg={270}>
        <Avatar icon={<ChevronIcon />} />
      </Rotate>
      <span>Up</span>
    </SpacedGroup>
  </SpacedGroup>
</Playground>

## API

<PropsTable of={Rotate} />
