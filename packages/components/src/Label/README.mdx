---
name: Label
menu: Components
---

import { Playground, PropsTable } from '@versionone/doc-components';
import { FormLabel, FormControlLabel } from './';
import { SpacedGroup } from '../SpacedGroup';
import { TextField } from '../TextField';
import { RadioGroup, Radio } from '../Radio'

A label used to identify a form field.

## FormLabel

<Playground>
  <SpacedGroup direction="vertical">
      <FormLabel labelPlacement="left" labelText="label to the left"><TextField /></FormLabel>
      <FormLabel labelPlacement="right" labelText="label to the right"><TextField /></FormLabel>
      <FormLabel labelPlacement="above" labelText="label above"><TextField /></FormLabel>
      <FormLabel labelPlacement="below" labelText="label below"><TextField /></FormLabel>
      <FormLabel disabled labelText="disabled label"><TextField /></FormLabel>
      <FormLabel required labelText="required label"><TextField /></FormLabel>
  </SpacedGroup>
</Playground>

## FormControlLabel

Used with a single control such as Radio or Checkbox

<Playground>
  <RadioGroup name="GroupName" defaultActiveRadio={0} defaultFocusedRadio={0}>
    <FormControlLabel labelPlacement="left" label="label to the left" control={<Radio />} />
    <FormControlLabel labelPlacement="right" label="label to the right" control={<Radio />} />
    <FormControlLabel labelPlacement="above" label="label above" control={<Radio />} />
    <FormControlLabel labelPlacement="below" label="label below" control={<Radio />} />
  </RadioGroup>
</Playground>

## API

<PropsTable of={FormLabel} />
<PropsTable of={FormControlLabel} />
